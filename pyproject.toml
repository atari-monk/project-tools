[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "project-tools"
version = "1.0.0"
description = "CLI tool with project-level tool set"

[project.scripts]
proj = "project_tools.cli:main"

[tool.setuptools.packages.find]
where = ["src"]